import React from 'react'
import { createDrawerNavigator, DrawerContenScrollView,DrawerItemList } from '@react-navigation/drawer';
import { View, Text } from 'native-base'

const CustomDrawer = (Drawer) => {
    console.log(Drawer)
  return (
    <View>
      <Text>Hola</Text>
    </View>
    
  )
}

export default CustomDrawer
